PORT=3000
OPENAI_API_KEY=your_api_key_here
OPENAI_BASE_URL=https://api.openai.com/v1
# Primary model for all 8 board members
MODEL_PRIMARY=gpt-4o-mini
# Fallback if rate-limited or temporary failures occur
MODEL_FALLBACK=gpt-4.1-mini
# Optional chairman override model (falls back to MODEL_PRIMARY)
CHAIRMAN_MODEL=gpt-4o
# Optional: where board can safely self-edit prompts/config
SELF_MOD_SCOPE=server/board/personalities.json
